use ndarray::{Array1, Array2};

mod activation_functions;
mod utils;

mod layers;

type Number = f64;
type Vector = Array1<Number>;
type Matrix = Array2<Number>;

fn main() {
    println!("Hello, world!");
}
